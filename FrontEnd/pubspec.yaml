name: lawgpt
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.9.2  # Make sure this version is compatible with your Flutter version


flutter_icons:
  android: true
  ios: true
  image_path: "assets/lgbt.png"  # Ensure this path correctly points to your icon file

flutter:
  uses-material-design: true
  assets:
    - assets/lgbt.png
    - assets/gpt.jpg
    - assets/law.png
    - assets/law1.png
